---
- name: Trigger cleanup after demo job
  hosts: all

  sources:
    - ansible.eda.range:
        limit: 1

  rules:
    - name: Demo trigger
      condition: event.i == 0

      action:
        run_job_template:
          name: Cleanup Job
          organization: Default
